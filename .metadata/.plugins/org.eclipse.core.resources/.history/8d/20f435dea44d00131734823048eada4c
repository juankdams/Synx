package SM;

import static SM.Sender.send;
import Game.DofusGameClient;

public class Crystal {

	
	
	/**
	 * Envoie packet @SC
	 * @param c
	 * @param packet
	 */
	public static void SC(DofusGameClient c, String packet){
		send(c.getSession(), "@SC"+packet);
	}
	
	
}
