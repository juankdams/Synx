package Parsers;

import Game.GClient;
import Plugins.Abstractions.AbstractPacketParser;

public class ConquestParser extends AbstractPacketParser{

	@Override
	public boolean parse(GClient c, String packet){
		switch(packet.charAt(1)){
			case 'b':
				return true;
			case 'B':
				return true;
		}
		return false;
	}

	
}
