import java.nio.ByteBuffer;
import java.util.ArrayList;

final class hM
{
  private final ArrayList aHr = new ArrayList(0);

  private hM(bxl parambxl) {  } 
  public void z() { this.aHs.aHr.clear();
    this.aHs.aHr.ensureCapacity(this.aHr.size());
    this.aHs.aHr.addAll(this.aHr); }

  private boolean e(ByteBuffer paramByteBuffer)
  {
    return true;
  }

  public void A() {
  }

  public boolean b(ByteBuffer paramByteBuffer, int paramInt) {
    if (paramInt < 0)
    {
      return false;
    }if (paramInt < 1) {
      boolean bool = e(paramByteBuffer);
      if (bool) {
        A();
        return true;
      }
      return false;
    }

    return false;
  }
}